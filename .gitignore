.DS_Store
Thumbs.db
*.log
node_modules/

# Keep local tool data out of the repo, but ship the frontend-design skill
.claude/*
!.claude/skills
.claude/skills/*
!.claude/skills/frontend-design

# Skills installed via `npx skills add`. skills-lock.json pins them, so the
# content is restorable with `npx skills experimental_install` rather than
# vendored into this repo.
.agents/
